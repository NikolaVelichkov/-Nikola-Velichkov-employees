<Window x:Class="EmployeesUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EmployeesUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition />
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition />
            <RowDefinition Height="20"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <DataGrid Name="MostWorkedDaysDataGrid" Grid.Column="1" Grid.Row="0" ColumnWidth="*">
            <DataGrid.Columns>
            </DataGrid.Columns>
        </DataGrid>
        <DataGrid Name="EmployeesDataGrid" Grid.Column="1" Grid.Row="2" ColumnWidth="*">
            <DataGrid.Columns>
            </DataGrid.Columns>
        </DataGrid>
        <Button Grid.Column="1" Grid.Row="4" Height="50" Width="100" Click="Compose">Upload</Button>
    </Grid>
</Window>
